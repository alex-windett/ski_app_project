<%= form_for [@run, @medium] do |f| %>
  <% if @medium.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@medium.errors.count, "error") %> prohibited this medium from being saved:</h2>

      <ul>
      <% @medium.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
  <div class="field">
    <%= f.label :video %><br />
    <%= f.file_field :video %>
  </div>
  
    <%= f.hidden_field :user_id, :value => current_user.id %><br />
  <!-- Dont need select run. Knows which run you have come from because of nested routes -->
  <br>
  <div class="actions">
    <%= f.submit "Add" %>
  </div>
<% end %>
